// Этот файл может быть почти пустым, так как основные настройки определены в settings.gradle.kts
buildscript {
    // Удалены репозитории, так как они определены в settings.gradle.kts
    dependencies {
        // Здесь можно добавить зависимости для Gradle, если требуется
    }
}

// Глобальные задачи или настройки, если нужны
// Удалена секция allprojects с репозиториями, так как они определены в settings.gradle.kts